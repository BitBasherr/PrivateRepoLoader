sync_now:
  name: Sync private repos now
  description: Trigger an immediate sync of all configured private repositories.
  fields: {}

reload_repos:
  name: Reload Private Repos
  description: Force refresh all private repositories and check for updates. This can be used from Developer Tools to reload integrations.
  fields: {}

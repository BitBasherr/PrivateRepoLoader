{
  "config": {
    "step": {
      "user": {
        "title": "GitHub token (optional)",
        "description": "Enter a Personal Access Token if you want it used as default for every repo you add later.",
        "data": { "token": "Personal access token" }
      }
    },
    "options": {
      "step": {
        "init": {
          "title": "Private repositories",
          "description": "Add, edit or remove repositories. Each entry needs its own token.",
          "data": { "repos": "Repositories" }
        }
      }
    }
  }
}

{
  "config": {
    "step": {
      "user": {
        "title": "Default GitHub token (optional)",
        "description": "Enter a personal-access token to use as default when adding repositories. Leave empty if every repo supplies its own.",
        "data": { "token": "Personal access token" }
      }
    },
    "options": {
      "step": {
        "init": {
          "title": "Private repositories",
          "description": "Add, edit or remove repositories below.",
          "data": { "repos": "Repositories" }
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Private Repo Loader is already configured."
    }
  }
}
